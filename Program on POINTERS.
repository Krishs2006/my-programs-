//Program on pointers.

#include <stdio.h>

int main() {
    int var = 20;
    int *ptr;

    ptr = &var;

    printf("Value of var variable: %d\n", var);
    printf("Address stored in ptr variable: %p\n", ptr);
    printf("Value of *ptr variable: %d\n", *ptr);

    return 0;
}
